pg_track_optimizer.mode = 'forced'
pg_track_optimizer.hash_mem = 4MB
# The extension's objects live in their own schema now (see
# pg_track_optimizer.control), so it must be on the path for the regression
# tests' unqualified references to resolve.
search_path = 'pgto, "$user", public'
